export interface Contact {
  name: string;
  number: string;
  id: string;
}

export interface ContactData {
  name: string;
  number: string;
}

export interface UserData {
  email: string;
  password: string;
}

export interface User {
  username: string;
  email: string;
  avatar: string;
}
