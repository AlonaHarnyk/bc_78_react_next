export interface Contact {
  name: string;
  number: string;
  id: string;
}
export interface User {
  age: number;
  name: string;
  isOnline: boolean;
  id: string;
}

export interface ContactData {
  name: string;
  number: string;
}
